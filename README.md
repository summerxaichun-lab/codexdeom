# Simple Spring Boot Demo

这是一个最简单的 Spring Boot 项目示例，提供一个 `GET /` 接口。

## 运行方式

```bash
mvn spring-boot:run
```

启动后访问：

- <http://localhost:8080/> -> `Hello, Spring Boot!`

## 测试

```bash
mvn test
```
